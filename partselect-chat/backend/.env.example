# PartSelect Chat Backend Environment Configuration

# Server Configuration
NODE_ENV=development
PORT=3001

# DeepSeek AI Service Configuration
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Development Notes:
# - Copy this file to .env and fill in your actual values1
# - Never commit the .env file with real credentials
# - The application will work in fallback mode without DEEPSEEK_API_KEY
# - Rate limiting: 100 requests per 15 minutes (900000ms) by default
# - CORS origins should include your frontend URL